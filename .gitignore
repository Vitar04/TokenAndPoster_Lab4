# Node / Next
node_modules/
.next/
dist/
.vscode/
.idea/
.DS_Store

# Truffle & build
build/
build/contracts/         # если хочешь, можешь оставить этот каталог — но он содержит артефакты
# Если нужно включить build/contracts (например, для артефактов), закомментируй строку выше

# Env / keys
*.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# Если хочешь коммитить package-lock.json — удали эту строку

# OS files
Thumbs.db
